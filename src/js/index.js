// El styles lo importamos aquí, ya se carga después al compilar todo
import '../scss/styles.scss';


//callback -> funcion que ejecuta otra funcion

const letters = ['a', 'b', 'c ', 'd']

letters.forEach(letter => console.log (letter))